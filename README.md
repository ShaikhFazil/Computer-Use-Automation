# Computer-Use-Automation
LLM-driven computer-use automation system for legacy banking UIs: an agent discovers a task once by driving a live app, records it as a typed, versioned capability artifact, then replays it deterministically (no LLM) with structured error handling, safety guardrails, and human-in-the-loop escalation for risky steps.
